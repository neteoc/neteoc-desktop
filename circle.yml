machine:
  node:
    version: 6.10.0

dependencies:
  override:
    - npm run setup

compile:
  override:
    - npm run package-linux
    - cp -R ./release-builds/* $CIRCLE_ARTIFACTS

deployment:
  production:
    branch: prod
    commands:
      - echo Getting Started
